/**
 * Tipos base para el cliente de Supabase.
 * En producción, reemplazar con tipos auto-generados via:
 *   npx supabase gen types typescript --project-id TU_ID > src/types/database.ts
 *
 * Por ahora definimos un type alias que permite tipar el cliente
 * sin bloquear la compilación.
 */

// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type Database = any
